spring.application.name=springboot-jpa

# puesto de conexion
server.port=8090

# Configuracion de la DB
spring.datasource.url=jdbc:mysql://localhost:3306/db_springboot
spring.datasource.username=root
spring.datasource.password=sasa1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
# Si esta sin crear la db, crea una vacia con los datos que le hemos indicado (solo para TEST)
# El problema es que la borra siempre que se inicia y la vuelve a crea, por lo que si tienes datos son eliminados
#spring.jpa.hibernate.ddl-auto=create
# Solo para actualizar cambios, no borra y regenera (solo para TEST)
spring.jpa.hibernate.ddl-auto=update